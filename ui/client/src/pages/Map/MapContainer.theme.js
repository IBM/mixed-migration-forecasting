const mapContainerTheme = theme => ({
  container: {
    padding: theme.spacing(4),
    // paddingBottom: theme.spacing(4),
  },
  plotly: {
    display: 'block',
    marginTop: theme.spacing(2),
    fontSize: 0,
    width: '100%',
    // height: 'calc(100vh - 12rem)',
  },
});

export default mapContainerTheme;
